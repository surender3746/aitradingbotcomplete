import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.surender.aitrading.bot',
  appName: 'AITradingbot',
  webDir: 'client/dist',
  bundledWebRuntime: false
};

export default config;
