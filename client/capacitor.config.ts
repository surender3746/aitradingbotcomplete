import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.aitradingbot',
  appName: 'AI Trading Bot',
  webDir: 'client/dist',
  bundledWebRuntime: false,
};

export default config;
